{
  "name": "git-snapshot-compare-gui-playwright",
  "private": true,
  "scripts": {
    "test": "playwright test --config=playwright.config.cjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
